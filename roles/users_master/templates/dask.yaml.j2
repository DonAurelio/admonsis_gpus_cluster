distributed:
  dashboard:
    # Locate the dashboard if working on a Jupyter Hub server
    link: "/user/{JUPYTERHUB_USER}/proxy/{port}/status"
    {# link: "{scheme}://{{ jupyterhub_public_ip }}:{port}/status" #}
